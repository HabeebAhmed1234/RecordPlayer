<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/abc_dialog_padding_material">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <EditText
            android:id="@+id/new_playlist_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/new_playlist_hint"
            android:layout_weight="1"/>
        <ImageView
            android:id="@+id/submit_new_playlist_btn"
            android:layout_width="@dimen/plus_btn_size"
            android:layout_height="@dimen/plus_btn_size"
            android:src="@drawable/ic_add"/>
    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/playlists_recycerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    </android.support.v7.widget.RecyclerView>

</LinearLayout>